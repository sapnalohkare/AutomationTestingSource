package collection.Map;

public class Theory

{
/*1) No Duplicates in Keys: Keys should be unique, but values can be duplicated.
 *2) Null Handling: It allows one null key in implementations like HashMap and LinkedHashMap, and allows multiple null values in most implementations.
 *3) Thread-Safe Alternatives: Use ConcurrentHashMap for thread-safe operations. Also, wrap an existing map using Collections.synchronizedMap() for synchronized access.
 *
 *
 *1) stores value in the for of key and value
 *2) key should be unique : value can be duplicate
 *3) null value accepted
 *4) does not maintain order of insertion
 *
 **/
}
