package collection.List;

public class Wait_long {

	/*In Java, the wait(long timeout) method is used for thread synchronization. It causes the current thread 
	 * to wait until either another thread calls notify() or notifyAll() for the same object, 
	 * or the specified timeout interval elapses
	 */

}
